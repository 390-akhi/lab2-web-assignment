# Task Manager API (Node.js + Express)

This project implements a basic REST API with tasks, health check, and error handling.

---

## 🚀 Features

- GET /tasks → returns 5 sample tasks
- GET /task/:id → returns a single task by ID
- GET /health → system health and uptime
- Error handling for:
  - Task not found
  - Invalid ID format

---

## 📂 Project Structure

